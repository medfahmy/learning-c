#include <stdio.h>
#include <math.h>

int main(void) {
    // int i = 10, j = 20;

    // double k = pow(double(i), double(j));
    // printf("%f\n", k);
    
    /*
    double base, power, result;

    printf("Enter the base number: ");
    scanf("%lf", &base);

    printf("Enter the power raised: ");
    scanf("%lf",&power);

    result = pow(base,power);

    printf("%.1lf^%.1lf = %.2lf", base, power, result);
    */

    // float x = -9.5, y = 12;
    // float mod = fmod(x, y);
    // printf("%.2lf\n", mod);

    int i = 0;
    printf("%d\n", i++);

    return 0;
}
