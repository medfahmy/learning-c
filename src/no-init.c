#include <stdio.h>

int main(void) {
    int a, b, c, d, e, f;
    float A, B, C, D, E, F;

    printf("ints %d, %d, %d, %d, %d, %d\n", a, b, c, d, e, f);
    printf("floats %.2f, %.2f, %.2f, %.2f, %.2f, %.2f\n", A, B, C, D, E, F);

    return 0;
}
