#include <stdio.h>

void print_joined(int arr[50]) {
    for (int i = 0; i < 50; i++) {
        printf("%d", arr[i]);
    }
}

int length(arr[]) {
    int i = 0;
    while (arr[0]) {

    }
}

int binary_search(int arr[], int n) {
    int l, r; 

}

int main(void) {
    int arr[] = { 2, 5, 6, 8, 9, 14, 25, 51, 87, 91 };
    int t = binary_search(arr, 51);
    int f = binary_search(arr, 11);
    printf("%d, %d\n", t, f);


    /*
    int arr[50];
    for (int i = 0; i < 50; i++) {
        arr[i] = i;
    }
    print_joined(arr);
    */

    return 0;
}
