#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void) {
    // char pwd[9];
    // int i;
    
    int n;
    printf("enter a number : ");
    scanf("%d", &n);
    system("clear");
    printf("you entered %d\n", n);


    // printf("enter password: ");
    // for (i = 0; i < 8; i++) {
        // pwd[i] = getch();
        // printf("*");
    // }
    // pwd[i] = '\0';
    // printf("\n");

    // printf("entered password: ");
    // for (i = 0; pwd[i] != '\0'; i++) {
        // printf("%c", pwd[i]);
    // }

    // getch();

    return 0;
}
